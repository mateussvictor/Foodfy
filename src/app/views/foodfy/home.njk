{% extends './layout.njk' %}

{% block content %}
  <section class="container">
    <div class="container-txt">
      <h1>As melhores receitas</h1>
      <p>Saiba como construir os melhores pratos
        <br/>
        com receitas criadas por profissionais
        <br/>
        de todo o mundo.
      </p>
    </div>

    <div class="container-img">
      <img src="./assets/chef.png" alt="chef"/>
    </div>
  </section>

  <main id="recipes">
    <h2>Mais acessadas</h2>
    <div id="cards">
      {% for recipe in recipes %}
        <a class="recipe-card" href="/recipes/{{ recipe.id }}">
        {% if not recipe.file  %}
          <img src="http://placehold.it/500x500?text=PRODUTO SEM FOTO">
          {% else %}
        <img src="{{ recipe.file.path }}" alt="Image of {{ recipe.title }}"/>
        {% endif %}
        <h3 class="recipe-title">{{ recipe.title }}</h3>
        <p>
      {% if recipe.chef_name == null %}por Chef Anônimo{% endif %}
      {% if recipe.chef_name != null %}por {{recipe.chef_name}}{% endif %}
       </p>
        </a>
      {% endfor %}
    </div>
  </main>
{% endblock %}
